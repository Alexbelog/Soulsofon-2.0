// Cloud sync configuration (Cloudflare Worker)
window.SOUL_CLOUD = {
  // Example: "https://your-worker.yourname.workers.dev"
  apiBase: "REPLACE_WITH_YOUR_WORKER_URL",
  // Optional: set to true to log cloud sync in console
  debug: false,
};
